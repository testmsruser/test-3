name: Action Sample

on:
  push:
    branches: 
      - master
  workflow_dispatch:

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: | 
         echo "Hello GitHub Actions!."
